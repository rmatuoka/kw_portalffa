<% title show_model_name(::Ticket, true) %>
<section class="content-header">
  <h1>
    <%= show_model_name(::Ticket, true) %>
    <br/>
    <small><%= t('activerecord.attributes.ticket.messages.index')%></small>
  </h1>
  <ol class="breadcrumb">
    <li>
      <a href="/admin">
        <i class="fa fa-dashboard"></i>
        Home
      </a>
    </li>
    <li class="active"> <%= show_model_name(::Ticket, true) %> </li> 
  </ol>
</section>

<section class="content">
  <p><%= submit_tag t('attributes.create_new'),:type=>"button",:onclick=>"window.open('"+new_admin_ticket_path+"','_self');", :class => "btn btn-primary"%> </p>
  <div class="box">
    <div class="box-body table-responsive no-padding">
      <table cellspacing="0" class="table table-hover" width="80%">
	      <thead> 
		      <tr>
			      <th style="width:35%;"><%=t 'attributes.name' %></th>	
			      <th style="width:20%;"><%=t 'activerecord.attributes.ticket.ticket_date' %></th>			
			      <th style="width:15%;"><%=t 'attributes.published' %></th>			
			      <!-- <th style="width:10%;"><%#=t 'activerecord.attributes.category.color' %></th> -->
			      <th colspan="3" style="width:30%;"><%=t 'attributes.actions' %></th>
		      </tr>
	      </thead> 
	      <tbody>
	      <% for ticket in @tickets %>
	      <tr <%='class=notactive' if !ticket.published%>>
		      <td><%= ticket.name %></td>
		      <td><%= I18n.localize(ticket.ticket_date) %></td>		
		      <td><%= print_active(ticket.published) %></td>
		      <!-- <td><div style="width:18px; height:18px; background-color:#<%#= category.color %>; border:1px solid"></div></td> -->
		      <td>
		        <%= link_to t('attributes.showing'), [:admin, ticket], :class=>"btn btn-default" %>
		        <%= link_to t('attributes.editing'), edit_admin_ticket_path(ticket), :class=>"btn btn-default" %>
		        <%= link_to t('attributes.exclude'), [:admin, ticket], :confirm => t('system_messages.notice_to_delete'), :method => :delete, :class=>"btn btn-default" %>
		      </td>
	      </tr>
	      <% end %>
	      </tbody>
      </table>
    </div>    
  </div>
</section>
