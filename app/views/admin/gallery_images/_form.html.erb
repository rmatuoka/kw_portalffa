<%= form_for [:admin, @gallery, @gallery_image] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :legend %><br />
    <%= f.text_field :legend %>
  </p>
  <p>
    <%= f.label :upload_id %><br />
    <div id="UploadImages">
    	<%@upload_images.each do |u|%>
			<%=f.radio_button :upload_id, u.id%> <%=image_tag u.file(:thumb)%> <br>
		<%end%>
    </div>
  </p>
  <p>
    <%= f.label :published %><br />
    <%= f.check_box :published %>
  </p>
  <p><%= f.submit %></p>
<% end %>
