<div id="Box_form">
<%= form_for ([:admin, @category, @subcategory]) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :template_id %>
	<%= f.collection_select :template_id,@templates,:id,:name, :include_blank => true%> 
  </p>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :published %>
    <%= f.check_box :published %>
  </p>
  <br />
  <p><%= submit_tag "Voltar",:type=>"button",:onclick=>"window.open('"+admin_category_subcategories_path(@category)+"','_self');", :class => "Bt_enviar", :style=>"margin-right: 5px;"%> <%= f.submit "Salvar", :class => "Bt_enviar" %></p>
<% end %>
</div>
